/**
 * BHUMI — Admin Panel JavaScript
 * Handles: sidebar toggle, delete confirmation, chart resize
 */

document.addEventListener('DOMContentLoaded', function () {
    // Nothing specific needed for admin at the moment.
    // ECharts initialization is handled inline in the dashboard view.
    // Delete modal is handled inline in the konten index view.
});
